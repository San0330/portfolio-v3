const SkillPill = ({ title }: { title: string }) =>
    <div className='zoom-in bg-white text-gray-500 rounded-xl border text-sm px-2 py-1 md:px-3 md:py-2 md:text-base tracking-wide'>
        {title}
    </div>

export default SkillPill
